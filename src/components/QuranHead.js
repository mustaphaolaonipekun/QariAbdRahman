import React from "react";
// import { createBrowserHistory } from 'history';
function  QuranHead () {
    // const history = createBrowserHistory();
    // const goBack = () => {
    //   history.goBack();
    // };

  return(
    <section>
            <div className="head">
                <i className="fas fa-arrow-left"></i>
        <p>Abdrahman قاري عبدالرحمن</p>
      </div>
    </section>
  )

}
export default QuranHead;
